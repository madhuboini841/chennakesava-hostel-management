{
  "name": "python-render-bridge",
  "version": "1.0.0",
  "description": "Bridge to boot Python Flask app on Render's Node environment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "python3 -m pip install -r requirements.txt"
  }
}
